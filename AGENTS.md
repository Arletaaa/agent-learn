# AI 管理指引

本文件定义了此学习笔记项目的管理规范。当用户要求你操作此项目时，请遵循以下规则。

## 目录规范

| 目录           | 用途         | 更新规则                                                                     |
| -------------- | ------------ | ---------------------------------------------------------------------------- |
| `daily/`     | 每日学习日志 | 每天创建`YYYY-MM-DD.md`，命名严格按日期格式                                |
| `concepts/`  | 知识点总结   | 每个概念一个文件，按学习顺序编号`NN-concept-name.md`，更新 `index.md`    |
| `projects/`  | 实践项目记录 | 每个项目一个子目录，包含`README.md`（项目概述）和 `notes.md`（开发笔记） |
| `resources/` | 资源收集     | 按类型分文件：papers / tutorials / frameworks / communities                  |
| `templates/` | 笔记模板     | 只读，不修改                                                                 |

## 文件命名规则

- 日志文件：`daily/YYYY-MM-DD.md`（固定格式，零填充）
- 概念文件：`concepts/NN-topic-name.md`（NN 为两位数字序号，topic-name 用英文短横线连接）
- 项目目录：`projects/project-NN-name/`（NN 为两位数字序号）

## 操作规范

### 引导式学习（默认规范）

学习新知识点时，**不要直接替用户写好完整笔记**，而是采用引导式流程。每次学习都必须按以下完整流程执行：

#### 阶段一：启动

1. **定位知识点**：从 `todo.md` / `roadmap.md` 取下一个未学习项，按阶段顺序推进

#### 阶段二：引导准备

2. **提供学习路径**：在对话中给出精简路径（分步骤，明确每步目标与使用资料）
3. **整理学习资料**：抓取官方文档 / 教程 / 论文，将**完整资料写入项目**（概念文档或日志），对话中只给精简要点。用户不需要自己上网找资料
4. **设计引导问题**：设计 3~5 个引导问题，覆盖核心概念与常见误区，让用户先自己思考回答

#### 阶段三：学习与反馈

5. **用户自学 + 回答**：用户阅读资料、回答引导问题
6. **纠偏答疑**：对用户回答逐条点评——正确的肯定，错误的纠正并补充深度；**问答记录必须写入当日 daily 日志**
7. **确认定稿**：用户确认理解后，概念文档才定稿（`concepts/NN-topic-name.md`）

#### 阶段四：收尾与推进

8. **同步索引**：更新 `concepts/index.md`、`learned.md`、`todo.md`、`roadmap.md`
9. **实践强化（可选）**：如知识点需要动手，布置实践项目，完成后记录到 `projects/`
10. **进入下一知识点**：回到步骤 1，循环推进

```
定位知识点 → 路径 → 资料入库 → 引导问题 → 自学回答 → 纠偏答疑(日志记录) → 概念定稿 → 同步索引 → 实践强化 → 下一知识点
```

> 原则：用户是学习主体，AI 是引导者。先路径、后资料、再提问，最后才整理笔记。
> 完整资料和每次问答记录都写入项目文档，对话中只呈现精简学习内容与引导问题。
> 若学习方式有变化（如用户选择"讲解式"），可临时调整，但默认遵守本规范。

### 创建每日日志时

1. 使用 `templates/daily-template.md` 模板
2. 文件放在 `daily/` 下，命名为当天日期
3. 如果当天有新的待学内容，同步更新 `todo.md`
4. 如果当天有新的已学内容，同步更新 `learned.md`

### 创建知识点文档时

1. 使用 `templates/concept-template.md` 模板
2. 分配下一个可用的序号（查看 `concepts/` 下已有文件）
3. 创建文件后，在 `concepts/index.md` 中添加条目
4. 在 `learned.md` 中添加对应条目

### 创建项目记录时

1. 使用 `templates/project-template.md` 生成 README.md
2. 在 `projects/` 下创建子目录
3. 项目完成后更新 `learned.md`

### 收集资源时

1. 将资源添加到 `resources/` 对应文件
2. 格式：标题 + 链接 + 简短说明（一句话）
3. 按分类整理，同类资源放在一起

## 更新规则

- 每次操作后检查 `todo.md` 和 `learned.md` 是否需要同步更新
- `concepts/index.md` 保持与 `concepts/` 内容一致
- `roadmap.md` 状态随进度更新（用 ✅ 标记已完成阶段）
- 不要删除已有内容，只做增量更新
- 不要创建未经用户要求的文件

## 语言偏好

- 所有笔记内容使用中文
- 技术术语保留英文原名
- 文件命名（非内容）使用英文
